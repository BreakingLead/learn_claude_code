# Default Blueprint for Runtime Migration

The first Agent Builder implementation will provide a simple default Agent Blueprint that mirrors the current Bee Agent behavior. This keeps the Web UI immediately useful, gives the resolver a concrete compatibility target, and lets the runtime migrate from hard-coded module wiring to Blueprint-driven assembly without changing the default user experience.
